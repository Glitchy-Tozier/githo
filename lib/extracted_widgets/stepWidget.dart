import 'package:flutter/material.dart';

class StepWidget extends StatelessWidget {
  final int stepIndex;
  const StepWidget({required this.stepIndex});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
